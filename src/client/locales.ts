/** Skin-manager settings row dictionaries. */

export const zh = {
  title: '皮肤',
} as const

export const en = {
  title: 'Skin',
} as const

/** Locale keys for the skin-manager row. */
export type SkinManagerKey = keyof typeof zh
